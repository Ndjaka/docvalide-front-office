import {Breakpoints} from "@mui/material";

const breakpoints : Partial<Breakpoints> = {
    values: {
            xs: 0,
            sm: 600,
            md: 1000,
            lg: 1300,
            xl: 1920
    }
};

export default breakpoints;